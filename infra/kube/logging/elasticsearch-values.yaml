# Elasticsearch configuration for NBank logging
replicas: 1
minimumMasterNodes: 1


# Resource limits
resources:
  requests:
    cpu: "100m"
    memory: "512Mi"
  limits:
    cpu: "1000m"
    memory: "2Gi"

# Volume configuration
volumeClaimTemplate:
  accessModes: ["ReadWriteOnce"]
  storageClassName: "standard"
  resources:
    requests:
      storage: 10Gi

# Security settings
createCert: false
secret:
  enabled: true
  password: "admin"

# Service configuration
service:
  type: ClusterIP
  httpPort: 9200
  transportPort: 9300

readinessProbe:
  enabled: false

# Environment variables
esConfig:
  elasticsearch.yml: |
    cluster.name: "nbank-logs"
    network.host: 0.0.0.0
    xpack.security.enabled: false
    xpack.security.transport.ssl.enabled: false
    xpack.security.http.ssl.enabled: false